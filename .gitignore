# Dependencies
node_modules/

# Build output
dist/

# Logs
*.log
npm-debug.log*

# Env / secrets
.env
.env.local
.env.*.local

# Internal docs — keep strategy/brainstorm notes out of the public repo
STRATEGY.md
SECURITY-BACKLOG.md

# Local Claude Code config (machine-specific, keep private) — but DO commit skills,
# so fresh cloud sessions get our reusable workflows (.claude/skills/).
.claude/*
!.claude/skills/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Normie Quest instrumented test build (dev-only, generated by build.js --test)
.nq_test.html
