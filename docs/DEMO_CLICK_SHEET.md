# Demo click sheet — what to click, in order (FF3, `docs/COLOSSEUM_ROADMAP.md` §16)

One page for the recording session. Full lines: `docs/DEMO_NARRATION.md`. Shot detail, captures,
honesty notes: `docs/DEMO_STORYBOARD.md`. Host: **`https://staging.clucknorris.app`** for every
row below — these pages are staging-only as of 2026-09-18 (`docs/HUB_VERIFY.md`'s "What's live
where" table); production (`https://clucknorris.app`) gets them only after the owner promotes
`develop` → `main`, at which point swap the host and nothing else changes.

| # | Time | URL (staging) | Say (one line) | Must be visible | Seed first |
|---|---|---|---|---|---|
| 1 | 0:00–0:18 | `/hub/demo/p/lock-to-earn` | "This is Demo Community's Lock to Earn program, version one — this hash is a sha256 of the terms above it, and anyone can recompute it. Holder A qualifies under the program's own term rule; Holder C doesn't, reason code term-too-short, printed in the open." | DRY RUN badge in the header; the hash next to the terms it was computed from; Holder A's QUALIFIES row and Holder C's red-bordered DOES NOT QUALIFY row both on screen | None — `/hub/demo` is a static fixture, always live |
| 2 | 0:18–0:31 | `/hub/demo/r/rcpt-a` | "This dry-run receipt names the wallet, the amount, and the transaction — a labelled fixture, nothing sent. A real receipt looks the same, with a live signature where this one says dry run." | "DRY RUN — no transaction was sent" label; wallet, amount, and the (fixture) transaction line | None — same fixture as above |
| 3 | 0:31–0:48 | `/hub/<real-project>/r/<sig>` — a receipt with retained inputs, "How this number was computed" expanded | "Every real receipt carries a 'How this number was computed' panel — not sales copy, the actual hourly slices this payment drew on, computed by the same function the ledger itself uses. This one shows seventy-three hours, summed to the exact amount paid." | The expanded `<details class="explain">` block; the per-hour rows summing to the paid amount; no DRY RUN badge (this is real, non-fixture) | Check a live `/hub/cuna/r/<sig>` first (`curl $HOST/api/hub/cuna` for a `sig`) — if it says "inputs were not retained," seed an internal, unlisted project per `docs/DEMO_STORYBOARD.md`'s "How the extra receipt was produced" note (no committed script; never present it as a live customer) |
| 4 | 0:48–1:06 | `/hub/verify`, "From saved files (offline)" tab, demo bundle dropped | "Drop this batch's evidence bundle onto the verify page: the bundle's own hash still matches, so the file was never altered. These two receipts report Missing Inputs, honestly, because this fixture predates the newer ledger model — not a failure dressed up as a pass." | "BUNDLE HASH: MATCHES"; two receipt rows reading MISSING_INPUTS — **do not show or read the "Program-version hash recompute" line** (fixed batch 12; irrelevant to this run) | `curl $HOST/api/hub-demo/demo/batch/demo-batch-1/bundle > bundle.json`, then drop `bundle.json` onto the page |
| 5 | 1:06–1:23 | `/hub/cmptest/programs/compare` | "Two program versions, side by side — their hashes, their published dates, and exactly what changed between them in plain words. A holder paid under the old one can see what the new one changes before deciding whether to stay locked." | Both versions' hash and published date; the two-row "what changed" table; the trust-boundary line at the bottom | `scripts/hub-compare-test.cjs`'s fixture construction (`cmptest`, two versions differing in lock term + payout cadence) |
| 6 | 1:23–1:35 | `/hub/wallet/4Gccq9pESbfNeKiW7M7qi587pYYiaQ4T4zLv3LcriGPs` | "One wallet, checked against every project on the platform at once — qualified and paid in one, excluded with a reason in another. Nothing here needed the wallet to connect." | "Seen in 2 projects"; one project's owed/arrived + settlement signature; the other project's exclusion reason in plain words | `scripts/hub-wallet-test.cjs`'s fixture (`walltest1`, `walltest2`, the same wallet in both) |
| 7 | 1:35–1:49 | `/api/hub/feedtest/feed.json`, then `/hub/feedtest/feed.xml` | "A JSON feed and its RSS twin — every version published, batch settled, and snapshot recorded, so a project can be followed with an ordinary feed reader, no wallet, no visit to the site at all." | Both raw responses rendered by the browser's own viewer (JSON Feed, then RSS); items newest first | `scripts/hub-feed-test.cjs`'s fixture (`feedtest`) |
| 8 | 1:49–2:00 | `/hub/glossary` | "Every term and reason code the receipts, the compare page and the wallet view use, defined once, in plain words, and linked from wherever the code actually appears." | The search box; the first terms with their "Learn this →" links | None — static content from `lib/hub/glossary.js` |
| 9 | 2:00–2:16 | `/hub/status` | "Every project this platform runs today, its programs, its reproducibility ratio over time, and the exact commit the running server was built from — plus the badge, computed live, that any project can embed in its own README." | The per-project DRY RUN pills (demo, POKEAHOE); the commit/branch line from `GET /api/build`; the badge card and its embeddable Markdown snippet | None required — shows whatever is really registered live; numbers will differ from the storyboard's seeded PNG, which is expected and more honest |
| 10 | 2:16–2:31 | `/hub/trust` | "And here's what none of that proves — in plain words, so nobody reads more into a hash or a receipt than it's earned. Educate, build, earn: the strongest version of earn is the one we can prove." | The full "what this does not prove" list on screen | None — static content, kept in step with `docs/HUB_VERIFY.md` §(g) by `scripts/hub-trust-doc-test.cjs` |

**Before recording:** `npm run build && node server.js` against a throwaway `DATA_DIR`, or point at
`staging.clucknorris.app` directly if the fixtures for rows 5–7 are already seeded there. Re-run
`node scripts/demo-storyboard-inventory-test.cjs` after recording if any new still is captured for
a future cut — it must stay green.
