# HANDOFF — 2026-09-18 evening (Colosseum build loop, slowed)

Written at the owner's ask ("we are going to lose fable, so we need to switch model and slow
down", 18:05 UTC). This is the state a session on any model needs to continue safely. Read
`CLAUDE.md` first; then `docs/COLOSSEUM_ROADMAP.md` §16–§17 for what is open.

## Posture now

- **Slowed.** One hourly wake (routine "Colosseum build loop (hourly at :50)"); the :20 twin is
  disabled. **One builder at a time**, `model: sonnet`, explicit `model:` on every Agent call.
  Opus only for the two lenses a money-path PR must have before merge. No new roadmap extension
  without an owner ask. Finish what is in flight before starting anything new.
- Everything else in the working agreement stands: `develop` is staging and merges on green CI;
  `main` only on the owner's explicit "promote" in the moment; fresh branch per batch, never a
  forced push, never `reset --hard`, never a branch delete of unmerged work; a disclosure line in
  `docs/PRE_EVENT_STATE.md` for every merged in-window PR, added as the next batch's first commit.

## Where things are (heads at 18:05 UTC)

| Branch / PR | State | What is on it |
|---|---|---|
| `develop` (= staging) | `b3ca3e2` (#356 merged) | Batches 1–17 (the rehearsal, this handoff, the re-timed scripts and the desk fix round included). The DD5 P1 fixes, the settlement journal as the live Hub payout path (#342), preview-before-publish, glossary, feeds, compare, print sheet, wallet roll-up, verify page, judge/status/trust pages, load proof, docs drift round 4, storyboard Parts 1–4. |
| `main` (= production) | `cfb2ce0` (#337) | Pre-Extension-5. **Every Hub surface since batch 9 is staging-only until the owner says "promote".** Recommended promote point: after batch 17 merges. |
| `claude/colosseum-batch-18` — PR opens on its first commit | open | Disclosure line for #356 only. Nothing else lands here until the owner asks; GG1 (the Sep 19 refresh) is the one dated item and would go here. **Recommended promote point: `develop` as it stands now (b3ca3e2).** |
| `claude/hub-browser-sign` — **PR #354** (money path) | fix round pushed as `5ffc393`; CI running; lens round 2 running | X6/CC5 browser-signed batch payout. Round 1 of both lenses said NOT merge-ready (`docs/HUB_BROWSER_SIGN_VERIFY_2026-09-18.md`); the fix round closed every finding with tests (30 server cases, 12 vm page cases). **Round 2 of both lenses is running on `5ffc393`**; their reports get appended to the doc. Merge only with zero open P0/P1 from both; if either finds a P0/P1, one more Sonnet fix round then re-lens. |

## In flight

A builder commits on its worktree branch and reports the sha; the session then `git cherry-pick -x
<sha>` (or `git merge` when the builder's last commit is a merge) onto the batch branch, resolves
i18n conflicts as key unions (keep each dictionary's byte format: es/zh two-space indent with a
trailing newline; hi/it/pt/vi single-line `', '` / `': '` separators, no trailing newline), runs
the suites on distinct ports, pushes, then `git worktree remove --force <path>` and deletes the
worktree branch. If this session dies, the worktree branches survive in the repo — `git branch
--list 'worktree-agent-*'` — and can be cherry-picked from a fresh clone after `git fetch`; they
are NOT pushed, so a container reset loses them.

No builders are running. The only agents in flight are the two read-only Opus lenses on PR #354 (round 2).

## Not started (Extension 11, in priority order once the above is in)

GG1 the Sep 19 refresh (`docs/ARENA_POSTS.md` round 4 for #352–#355; regenerate
`docs/WEEKLY_UPDATE_2026-09-20.md` with `scripts/weekly-update-draft.cjs` the night before Sep 20;
tick the E9 checklist) — **this one has a calendar deadline**. GG2 the operator-surfaces lens
(Opus, read-only, then a Sonnet fix round). GG4 wallet feeds. GG5 the holder on-ramp strip.

## Waiting on the owner (mention only at boundaries)

"promote" (recommended after #355); npm publish of `packages/hub-verify`; the go for the
weekly-update routine (EE4); a Higgsfield voice for the demo voiceover (nothing generated yet);
the POKEAHOE terms with the team (template in the rehearsal doc, no numbers).

## Traps this session hit (so the next one does not)

- A trust-page or verify-page sentence edited in English without its six translations fails the
  i18n audit in CI (`scripts/i18n-audit.cjs`, gating for the Hub pages it lists). Translate, do not
  reword around it.
- `git worktree remove` while a background command is still using that worktree loses uncommitted
  work. Wait for the builder's report first.
- The CI workflow cancels an in-progress run when a newer push lands on the same PR; a poller keyed
  on the old sha reads "cancelled". Check the run for the current head.
- Staging (`staging.clucknorris.app`) is behind Cloudflare Access; a cloud container cannot reach
  it. Rehearse against a local boot of the merged tree and name the staging URLs in the doc.
- `packages/hub-verify` drifts whenever a copied lib or schema changes: run
  `node scripts/build-hub-verify-package.cjs` and commit the regenerated files.
