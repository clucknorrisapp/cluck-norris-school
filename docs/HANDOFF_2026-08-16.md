# HANDOFF — 2026-08-16 (Normie Quest: boss "sunk in floor", resolution, speed)

Read this if you're picking up the **boss look / character speed** thread. It carries the one
finding that cost ~24 hours (the boss "sunk into the floor" bug is an **ART crop, not a position or
resolution bug**), what shipped, and the two things still open. Also read
`docs/HANDOFF_2026-08-02.md` and `docs/HANDOFF_2026-07-27.md` for the older Normie Quest state.

> ✅ **STATUS UPDATE — 2026-08-16/17: THE OPEN ITEMS BELOW ARE RESOLVED.** The KOL and Custodian
> plates were replaced with full-body art (the bosses have feet), the grounding-shadow mitigation
> was deleted (`grep _shadow normie-quest/src/game_logic.js` → nothing), and the small real position
> bug underneath it was fixed (boss body bottoms are now 1.00; regression guard:
> `node normie-quest/test/nq-boss-ground.cjs <baseUrl>`). Resolution: 3× on desktop but **2× on touch
> devices** (`game_logic.js` ~L25, 2026-08-17), so "settled: RES=3" below is only half the story.
> The TL;DR, the "Grounding shadow" bullet under "What shipped" and OPEN ITEMS §1 describe the
> pre-resolution state and are kept as history; CLAUDE.md's 08-16 block is current.

Production right now: **`main` = `develop` = `982144d`, live on clucknorris.app (deploy verified).**
**Not yet promoted:** `claude/normie-quest-background-zoom-o1r78z` carries the background fix
(`d326ad6`) and the 3× resolution raise (`5350d52`). Both need merging to `develop` for staging, and
an explicit owner go before `main`. Neither has been on a real phone yet.

---

## TL;DR — the answers the owner kept asking

- **"Is the boss fixed?"** Partially. A grounding shadow shipped (reads as standing, not buried), but
  it's a *mitigation*. The boss art still has no feet. **Not fully fixed** until the art has feet.
- **"Did you make a new image for the KOL so he has legs?"** **No.** Not done. This is the real fix
  and the top open item (see below).
- **"Did you fix the backgrounds?"** **YES — fixed later the same day.** ⚠️ The two bullets that used
  to sit here said no regression existed and that resolution was a no-op on screen. **Both were
  wrong.** The owner was right: the backdrops really were zoomed in, and 3× really was worse than 2×.
  See "THE BACKGROUND FINDING" below — it is now proven on screen, not argued.
- **"Undo the 2× resolution thing."** Done (locked to 2×, `?res=` switch removed). It was **not** a
  no-op: RES scaled the backdrop crop. That coupling is now gone, so RES is a pure sharpness knob.

---

## THE BACKGROUND FINDING (fixed — `d326ad6`, don't re-derive)

**The world backdrops were showing a blown-up CROP of the plate, not the plate.** The centre 1/4 of
it at RES=2, the centre 1/9 at RES=3. On 3-1 that was a close-up of two buildings — the moon, the
skyline tops and the whole rooftop foreground were off-screen. The owner reported this repeatedly and
it is real; an earlier pass this day argued it away as unreproducible. It is trivially reproducible:
screenshot any `bgArt` level and compare with `normie-quest/public/worlds/<plate>.webp`.

**Cause — the trap to remember:** `setScrollFactor(0)` stops an object SCROLLING; it does **not** take
it out of the camera transform. A zoomed camera still scales screen-pinned objects about its viewport
centre: `screen = half + zoom*(p - half)`, `half = cam.width/2`. So the backdrop's
`add.image(0,0).setDisplaySize(cam.width, cam.height)` drew RES times oversized and anchored
off-screen, leaving only the middle `1/RES` of each axis visible. **This is also why the HUD slid off
at 3×** — `hudBox` was hardcoded at `(W/2,H/2)`, which is the correct anchor at RES=2 and only at
RES=2.

**Fix:** `SCREEN_RECT(cam)` in game_logic.js (defined next to `RES`, with the full note) returns the
rect in scrollFactor-0 coords that maps to the whole canvas at any zoom. The backdrop, its scrim, the
HUD container and the lab heatmap caption all place through it. **Use it for any new screen-pinned
object** — never hardcode, never use `(0,0)`+`cam.width`.

**Verified:** framing is pixel-identical at RES 1, 2 and 3, and the HUD stays put at all three.
`nq-visual.cjs` passes unchanged, with `hud` at diff **0%** — i.e. the HUD refactor is provably
behaviour-identical at the shipped 2×, so no baselines needed re-approving.

**Consequence — the resolution question is settled: `RES=3` (owner ask, `5350d52`).** RES no longer
touches framing, only sharpness. 1× is visibly chunky, 2× was fine, 3× resolves the cap lettering,
sunglasses and sneakers — this is the change the 08-15 session attempted and had to revert, and it
only works because SCREEN_RECT removed the coupling. Visual baselines were re-approved at 3×; the new
`hud.png` baseline showing every element in place is the direct refutation of "3× breaks the HUD".
Gate stability was re-measured at 3× (second identical run vs the fresh baselines): gravemite **0%**,
all three character surfaces **0%**, hud 0%, title 1.3% — steadier than the 2× baselines were, so no
threshold was loosened. ⚠️ Cost is **2.25× the fill rate** vs 2×; **`RES` is the one number to drop if
a low-end phone struggles** — nothing else depends on it. Not yet checked on a real phone.

---

## THE finding (don't re-derive this — it cost a day)

**The bosses look "sunk / waist-deep / legs under the pavement" because the boss cutout art is drawn
from the THIGHS UP — there are no feet.** The boss's *position is correct* (body.bottom sits exactly
on `GY`, verified by measurement); the sprite simply has no feet to show, so its legs terminate at the
pavement with nothing below — which reads as buried, worst of all right next to Normie, whose art has
real shoes on the ground.

Hard evidence (opaque-pixel bounds of each texture, fraction of texture height where content ends):

| texture (src/assets/) | content bottom | feet drawn? |
|---|---|---|
| `cut_normie_idle.b64` (player) | 99.1% | ✅ shoes at the very bottom |
| `cut_jeet.b64` (the green goblin) | 100% | ✅ |
| `cut_bot.b64` (enemy) | 100% | ✅ |
| `cut_scammykol.b64` (KOL boss) | **96.4% — cut off at the thighs** | ❌ no feet |
| `cut_troll.b64` (boss) | 95.6% | ❌ |
| `cut_ceoboss.b64` (boss) | 97.2% | ❌ |

**Every "cut_" BOSS cutout has this. Enemies and the player do not.** This is why 24 hours of floor
clamps, penBoss tweaks, body-offset changes, and position measurements never moved it — the position
was always right. Do not chase position again.

**How to re-measure any texture:** decode the `.b64` (strip any `data:` prefix, base64-decode to PNG),
then in a headless canvas find the min/max opaque rows. The boss/player art has been this way since
the repo's git baseline (5f80105, 2026-08-12) — it is NOT a recent regression, but it IS a real bug.

---

## What shipped this session (all on `main`/`develop`)

- **`982144d`** — the final ship:
  - **Grounding shadow** under every *gravity* boss. Added once in `penBoss` (game_logic.js ~L3237):
    a soft `add.ellipse` at `GY+3`, width `displayWidth*0.72`, alpha 0.36, depth 6, tracks the boss's
    x, pinned to the floor even mid-leap. Freed in `bossDefeat` (`k._shadow.destroy()`). Flying bosses
    (`allowGravity=false`) cast none. **It's a mitigation, not the fix.**
  - **Resolution locked at 2×.** Removed the `?res=` URL override (was game_logic.js L15); `var RES=2`
    stays. ⚠️ This bullet used to claim the switch was "proven a no-op on screen". **That was wrong** —
    RES scaled the backdrop crop and the HUD anchor, which is exactly what the owner was seeing. See
    "THE BACKGROUND FINDING" above. Locking to 2× was still the right call at the time (it closed the
    path to the broken 3×); it just wasn't harmless, and it wasn't the whole story.
  - **`__NQ_BOSSVIEW`** — new setup-lane lab hook: seats the player next to the boss and locks the
    camera on the pair, so a boss-render bug can be screenshotted (the camera is otherwise stranded at
    the level start after `__NQ_FORCEBOSS`).
- Earlier this session (already live): `01e0caf` giga-chad speed fix, `d4e5672` audio resume,
  `8dfcf57`+`cca61e1` Rug King floor clamps, `67f03d3` Rose buy-bot sells-as-buys fix.

---

## OPEN ITEMS (in priority order)

### 1. Give the bosses real feet — THE fix the owner wants
**Started. Art generated, awaiting the owner's pick — and blocked on a network-policy detail.**

Three full-body KOL candidates were generated with Higgsfield `nano_banana_pro` from the existing
`cut_scammykol` cutout as reference (job ids `ae39269a…`, `2a201eb4…`, `b69cbd09…`; shown to the owner
via the generation widget on 2026-08-16). Measured objectively, not eyeballed: all three carry two
separate legs, a wide stance and chunky sneakers, with opaque content reaching the **very bottom edge**
of the frame (gap below content = 0px). Palette matches the original (magenta shirt, royal-blue denim,
navy outline). Final sprite crops to **330×480** (the original is 330×360 — the extra 120px is legs).
Note the original already ran edge-to-edge horizontally (L/R margins = 0), so arms touching the frame
sides is faithful, not a new crop bug.

⛔ **THE BLOCKER — an EGRESS-POLICY CHANGE, not a limitation of the method.** ⚠️ Do not conclude from
this that art can't be pulled into the repo from a cloud session. It normally can, and
`docs/MEDIA_LIBRARY.md` says so explicitly ("CDN URLs are long-lived … downloadable from any fresh
session, `curl -o file.png "<rawUrl>"`") — that is exactly how the Normie Princess sprite landed on
2026-07-21. **Re-test before assuming you're blocked.**

In the 2026-08-16 session, this environment's egress policy 403'd **every** Higgsfield host at the
CONNECT stage — `higgsfield.ai`, `api.higgsfield.ai`, `upload.higgsfield.ai`, and both result CDNs
(`d8j0ntlcm91z4`/`d2ol7oe51mr4n9.cloudfront.net`). The MCP tools kept working throughout, because they
travel over the MCP connection rather than the container's network; that asymmetry (can generate, can't
download) is the tell. `/root/.ccr/README.md` is explicit that a proxy 403 is an org egress denial and
must be **reported, not routed around** — a headless-Chromium fetch was tried and correctly hit the
same wall.

**Fix: allow those hosts in the environment's network policy** (Claude Code on the web → environment
settings; see https://code.claude.com/docs/en/claude-code-on-the-web). Interim path that needs no
settings change: the owner downloads the chosen PNG from the generation widget and uploads it into
`normie-quest/src/assets/` via GitHub's web UI, then the session pulls and processes it.

Useful regardless: **Higgsfield `sandbox_exec` reaches the public site**, so
`curl https://clucknorris.app/normie-quest-x7` + regex `scammykol:'data:image/png;base64,…'` extracts
any live game sprite without needing the repo. That is how the reference image was built.
Do **not** try to carry image bytes back through the transcript as base64 (~66–190 KB) — one bad
character silently corrupts the sprite.

Once the PNG is in hand: strip the flat grey background (flood-fill from the corners, fuzz ~18%),
trim to content, resize to 330 wide, base64 it into `normie-quest/src/assets/cut_scammykol.b64`, then:

Older notes on the approach:
- **Higgsfield** (the tool that made the originals — MCP server, available in-session) — `outpaint_image`
  to extend the bottom of each `cut_<boss>.b64` with legs + shoes in the same pixel-art style, then
  re-crop to the content box so the existing body config still lines up, and rebuild.
- Start with **`cut_scammykol.b64`** (World 3 boss, what the owner is staring at), show the owner
  before touching the other ~10 boss cutouts.
- After swapping art: the body config at the KOL spawn (game_logic.js ~L3700,
  `setSize(w*0.60,h*0.82).setOffset(w*0.20,h*0.14)`) assumes the OLD crop — re-tune the offset so the
  new feet land on `GY`, and the grounding shadow can likely be removed or kept as a subtle extra.

### 2. Characters move too fast, especially with powerups — owner wants to "check settings"
**In-flight, not resolved.** The speed knobs (game_logic.js):
- **Base** max velocity: **240** (L2529 initial + L5527).
- **Powerup** tier (caffeine / moon / GIGA CHAD omega): max velocity **340** (~1.42×), accel **1500** (L5527–5528).
- **Slow** (FUD gas): 120.
- ⚠️ Earlier this session `01e0caf` **raised giga chad from 240 → 340** to fix "giga chad gets slow."
  The owner may now feel the powerup speed (340) is too fast. **Get the owner's target before changing**
  (e.g. drop the fast cap 340→~290, and/or accel 1500→~1200) — this is a feel/tuning call, his to make.
  Don't also slow base 240 without asking; it sets level reachability (the geometry checker uses it).

### 3. Backgrounds "look off" — ✅ FIXED (`d326ad6`)
See "THE BACKGROUND FINDING" above. Nothing left open here. (The note that the plates paint a large
dark rooftop foreground with a thin floor strip at the bottom still holds — that IS the plate design.
With the full plate now on screen it reads as intended rather than as a void.)

---

## Tools / gotchas for continuing

- **Build:** `node normie-quest/src/build.js` inlines `src/game_logic.js` + swaps `__MARKER__` art
  tokens for the `.b64` data-URIs, writing `public/normie-quest-platformer.html` (+ `-play.html`).
- **Serve locally:** `PORT=3111 node server.js` (serves the built HTML fresh each request — no restart
  needed after a rebuild). Lab lane: `/normie-quest-x7-lab`.
- **Reach the scene in headless:** `window.Phaser.GAMES` is **undefined** in this build. Use only the
  `window.__NQ_*` hooks (`__NQ_STARTLEVEL`, `__NQ_FORCEBOSS`, `__NQ_BOSSVIEW`, `__NQ_SHOVEBOSS`,
  `__NQ_DBG`, `__NQ_RECT`). `__NQ_RECT('boss'|'player'|'<texkey>')` returns screen-fraction rects
  (feet = `y+h`) — the same probe the visual gate uses.
- **Headless does NOT settle a forced boss's fall** — it stays at spawn `y`. After `__NQ_FORCEBOSS`,
  call `__NQ_SHOVEBOSS(0,222)` to seat the KOL on the floor (body.bottom→GY), then screenshot.
- `nq-visual.cjs` and `nq-verify.cjs` are **slow / hang** in this heavy-headless container (spawn ~12
  browsers). `node normie-quest/test/nq-geometry-check.cjs` is fast and static — use it. The visual
  gate's only HARD gate is the gravemite; character/HUD surfaces are advisory (flaky).
- **Constants:** `W=480 H=270 TILE=24 GY=246` (floor top). Boss depth 7, player 7, shadow 6, floor 0,
  bgArt −58.

## Standing constraints (unchanged)
- **Rose buy bot: STAY DISARMED** until the next buy comp (owner). Not touched this session.
- `develop` → staging, `main` → production. `main` is a **production release**; promote only on an
  explicit owner go. (This session the owner said "you have permission to send the change live.")
- Never commit secrets or a model identifier.
