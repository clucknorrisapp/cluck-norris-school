# Media Library — brand asset manifest

**Why this exists:** cloud sessions are ephemeral and Higgsfield generations pile up fast.
On 2026-07-08 we burned a regeneration cycle because "the good video" couldn't be located.
Rule (also in CLAUDE.md): **every KEPT generation gets a row here — job ID, CDN URL, what it
is, and the owner's verdict.** Before regenerating anything, check this file and
`show_generations` first; before overlaying/editing, confirm the exact job ID with the owner.

CDN URLs are long-lived Higgsfield/CloudFront links — downloadable from any fresh session
(`curl -o file.mp4 "<rawUrl>"`). The scratchpad copies die with the container; these don't.

## Reference images (mascot identity — pass as `medias[{role:image}]` for on-brand renders)

| ID | What | Notes |
|---|---|---|
| `387db388-7db0-4635-a478-8b4d6c7fb4a2` | Branded Cluck reference (muscular rooster, shades, red mohawk, LP tactical vest, ammo belt) | Used for `47624c22`. Describe WITHOUT weapon words in the prompt or Seedance NSFW-false-flags (refunded, but slow). |
| `b1f7713b-69fc-4f18-bed7-781c6e9bb50f` | Branded Cluck reference (alt) | Used for `3c51927a` (owner-loved look) and `4a3ee7a5` (clean-wall hero). |

## Videos

| Job ID | What | Verdict / status |
|---|---|---|
| `4a3ee7a5-eb5d-441b-bf17-5bc324e68af9` | **/learn hero candidate** — 15s 1080p grand marble hall, branded Cluck at podium, CLEAN constellation wall (zero coins), "LEARN EVERY CHAIN" banner, ends on crisp `clucknorris.app/learn` plaque | ✅ Current best. Logo treatment PENDING owner decision (end-card recommended). `rawUrl: hf_20260708_215024_4a3ee7a5….mp4` |
| `3c51927a-e02d-4de8-976f-14aa88e6deb0` | 15s grand-hall, owner-LOVED cinematography, but wall coins are WRONG (duplicate ETH, hallucinated asterisk coin) | ❌ Unusable as-is; coins are baked-in at depth on a moving shot — not fixable by overlay. Its *look* is the bar to hit. |
| `47624c22-3c05-4f75-8b22-6f65a70001a6` | 15s library, branded mascot, clean wall — flat/static staging | ❌ Owner: "this really sucks" (after logo overlay). Too flat vs `3c51927a`. |
| `278f637a-7230-4946-9b3b-1e8890e0aa99` | 15s wholesome professor rooster (no reference image) | ❌ Owner: "looks horrible, like a play chicken." NEVER generate mascot without a reference image. |
| `621d2051-87c3-4681-9bee-0845df98cd27` | 8s 720p original lecture-hall ("crazy good") = the owner's `learnlaunch.mp4` | ✅ Owner-praised; superseded by 15s efforts but the mascot/energy benchmark. |
| `4ff196a4-afae-496e-9b3b-dc109a8168ab` | 8s lock-celebration (Cluck carries bag to CLKN locker) | Used 2026-07-03. |
| `2c382434-c41d-4caf-8f91-ae739104da2e` | 5s Cluck eats chicken sandwich (diner) | Fun one-off. |
| `8373e328-46b3-4c3a-ba90-87a6f57215ca` + ffmpeg post | **$600K market-cap celebration 2026-07-13**: rooftop fireworks + chart-constellation + confetti-cannon Cluck, rendered TEXT-FREE, then cropped (bottom 120px removed a gibberish stone engraving) + Oswald-700 "$600K MARKET CAP" / "clucknorris.app" overlays. Final CDN: `32356dab-3ee7-4c47-b2b6-5858c936d741.mp4`. Posted X `2076731096054583522` (first NATIVE X video via new chunked upload) + TG 86241. Two dead takes: `8ac98853` NSFW-flagged (champagne bottle = alcohol trigger, refunded), `9ac86c38` banner typo "6700K". | ✅ The working video recipe: render text-free → stamp text with ffmpeg/Pillow. |
| `0910b815-49b3-4abe-b6fc-73df32102540` + ffmpeg post | **Cluck Norris BOT patrol — OWNER-APPROVED FINAL 2026-07-17 ("love it")** (roll 7 — supersedes rolls 5-6 below): same roll-6 visuals, but ALL audio native Seedance in one pass — trailer structure: background FEMALE NARRATOR mid-video ("In times of uncertainty, look for guidance in the right areas") then Cluck speaks to camera in Seedance's own deep robotic voice, beak-synced ("Stay strong. I am here to guide you"). The post-dubbed ElevenLabs/robotized attempts (rolls 5-6) were REJECTED by the owner — Seedance's native voice beat our dub; ask for dialogue in-prompt with exact quoted lines + voice description. Post = lettering overlay only, audio copied untouched. 17.2MB. Hosted CDN: `e53fdf8f-87f3-4810-ac0c-230d9612de17.mp4`. Sent to owner's private bot chat (msg 611). **PUBLISHED (owner's go): X `2078156426887762193` (native video) + TG community 86802 (native video, silent, with X link).** Bump self-reply still owed a few hours after post time (~16:35 UTC 2026-07-17) per the standing X bump rule — scheduler unavailable in-session, do it manually. | ✅ THE final. **Voice law: Seedance native dialogue > post-dub for character speech; give exact quoted lines + per-voice tone descriptions and it casts them correctly.** |
| `48bf4457-946f-4c44-9c0b-637954ebf046` + ffmpeg post | **Cluck Norris BOT patrol 2026-07-17** (roll 6 — superseded by roll 7 above): owner flagged roll 5's chart lines still reading "up", so roll 6 removed the words chart/line/graph from the prompt entirely — screens show ONLY huge red minus-percentages ("-23%/-38%/-41%") + giant down arrows, which Seedance renders perfectly. Voice made ROBOTIC per owner ask: the `/api/tts` Cluck line run through ffmpeg DSP (pitch ×0.88 + tremolo f=35 ring-buzz + metallic aecho + compressor) — recipe in scratchpad history, re-derivable. Same lettering overlay + dub chain. Final 17.4MB. Hosted CDN: `90e81a98-d3d0-44e7-9297-7a2074184fa5.mp4`. Sent to owner's private bot chat (msg 610). | ✅ THE keeper. **Chart-direction law, final form: don't ask for charts at all — numbers + arrows only.** |
| `355f3c5e-571e-4651-904d-a8c526afe0b9` + ffmpeg post | **Cluck Norris BOT patrol 2026-07-17** (roll 5 of 5): 15s 1080p, emblem burst → neon plaza market crash ("-38%" plates everywhere, red boards, panicked suits) → bot calmly directs crowds (bare graphite hands, zero VFX) → face-camera fists-on-hips fire-ring hero ending. Post: Anton "CLUCK NORRIS / clucknorris.app" fade-in @11.8s + REAL Cluck ElevenLabs voice dub @11s ("Stay strong. I am here to guide you through the market turmoil." — synthesized free via prod `/api/tts`, native audio ducked to 0.30) replacing the rooster crow. Final 18.6MB (fits TG's 20MB URL limit). Hosted CDN: `d7ed593a-d67a-4f19-986b-c1cf2e550be4.mp4`. Sent to owner's private bot chat (msg 609). Dead rolls: `3d915c61` (crow + spark-zap hands + up-charts), `4c42bb9b` (flame-jet hands), `c7704055` ("traffic officer" simile spawned an actual cop who hijacked the ending), `91e010ab` ("no police" negation FILLED the plaza with cops; "no upward bounces" spawned an up arrow). | ✅ **PROMPT LAW (5-roll proof): VFX negations work ("nothing emitted from hands — no fire, no sparks") but ENTITY negations literalize ("no police" → police everywhere) — never name an object/person that must not appear; describe the crowd/scene positively instead. Occupational similes spawn characters. Seedance CANNOT draw a descending chart line — sell a crash with "-38%" plates + down arrows, not chart direction.** |
| `846890b6` (intro) + `85d20bd0` (finish) + beats A/B/C + ffmpeg | **25s LOCK celebration 2026-07-22** (+5.00M → 41.87% of supply / 56 escrows): owner asked for a LONGER lock video. Extended the approved 3-beat kick→throw→lock cut with an epic Seedance 2.0 intro (Cluck strides into the vault chamber, `end_image`=lock-still `935e3024`) + a sealed-vault fist-raised finish (`start_image`=beatC_end `c548a8fc`), normalized + concat to 25.26s, Oswald-700 stats lower-third fading in after the intro. NOTE: this container's static imageio-ffmpeg has NO `drawtext` filter — rendered the 3 lines to a transparent PNG with Pillow and composited via the `overlay` filter (fade=in:alpha). Hosted CDN `8e579187-19e1-4ca6-85e1-c63c1353e292.mp4`. Posted X `2079741823807865288` (native video, @JupiterExchange + @BagsApp) + TG community (silent mirror w/ X link). | ✅ Pushed on owner's go. Lock text had already auto-posted via the 24h fallback — this video is the follow-up. |
| `5e3109d3-7569-416a-b831-5ec43fff7cbf` + ffmpeg post | **LOCKER ROOM launch 2026-07-16**: 15s 1080p vertical (9:16) cartoon Cluck slams a giant vault, coins pour in, sparks (Seedance 2.0, `genre=action`, native audio, START-FRAME = cartoon keyframe `868a9a33` which was built from mascot ref `b1f7713b`). Rendered TEXT-FREE, then ffmpeg-composited two Anton cards rendered via headless-Chromium PNGs: title "LOCKER ROOM / Solana's free token locker" (early) → end card "LEARN LOCKS / LOCK YOUR SOLANA TOKENS / NO EXTRA FEES · NO TOKEN CUT / clucknorris.app". Hosted CDN (uploaded via media_upload): `4b1eca63-5442-4a0a-9a00-d25bfd9f06cc.mp4` (13.7MB). Posted X `2077852171094122881` + TG community 86708 (both with native video). | ✅ Launch video for the Jup Locker Room. Recipe reconfirmed: render text-free → overlay Anton PNGs with ffmpeg. |

## Images / composites

| `b469ffab-fd0a-4828-b184-22d8c3df3511` (idle) · `386c82f8-2125-49f0-ad24-83e7b95b74a4` (run1) · `82f6637d-e22f-4335-9963-799081b5f86c` (run2) · `0527ddcd-c3d4-45cb-b34f-9198b4ac6b1e` (jump) → cutouts `b63532fd` / `92b0fcda` / `af3af6a4` / `a0c710bd` | **ASTRONAUT NORMIE — World 21 moon suit, 4-frame sprite set 2026-07-24** (Normie Quest). nano_banana_pro, each frame generated from the matching ORIGINAL Normie frame as pose reference PLUS the idle astronaut as character reference — that dual-reference trick is what kept the suit/helmet/patch identical across all four. White suit, clear bubble helmet w/ gold rim, green frog face + dreads + shades visible, red NORMIE chest patch, backpack, moon boots. Post-processed locally: bg removed, then each frame fitted into its ORIGINAL frame's alpha bbox on the shared 144×216 canvas so the ground line + centre match frame-for-frame (no animation jitter). Shipped as `cut_normie_moon_{idle,run1,run2,jump}.b64`. | ✅ In game (worlds 21-1/2/3 via `def.moon`). |
| `14548f82-e60f-4683-ab65-a16655a09779` → cutout `f4c098bd-5d57-4983-b58a-1805f7965ac2` | **WEN MOON boss sprite 2026-07-24** (Normie Quest World 21 FINAL boss, replaces Saylor as the ending): cratered full-moon head w/ smug grin + orange laser eyes, cosmic starfield body w/ cyan cracks, gold crescent chest emblem, crescent-blade staff. Fitted to 330×360 to match the other boss plates. Shipped as `cut_wenmoon.b64`. | ✅ In game (`bossType:'wenmoon'`, VIP_BOSSES entry). |
| Mega whale gen `67707b2e-fdf2-4084-a35f-15d77c78006b` → cutout `fbdfe910-e9f1-467e-9748-53229c411b21` → `cut_whalemount.b64` | **MEGA WHALE RIDE MOUNT 2026-07-27** (owner: "make a better whale to ride for the mega whale power boost"). The `megawhale` top-tier holder perk is a timed invincible flying ride; the mount had been a crude 80×44 programmatically-drawn vector whale (`generateTexture('whalemount')` in `create()`). Now a real sprite — a regal crowned blue whale, side profile facing RIGHT, **gold crown + gold fin-trim** to signal the ELITE mega-whale tier, spout, two-lobe tail fluke on the left. nano_banana_pro on a flat-green key, bg-removed, contain-fit to **480×264 (0.55 aspect)** to match the ride's `setDisplaySize(ww, ww*0.55)` so it isn't distorted. Loaded via `SPRITES.whalemount` (boot `addBase64`) + a build marker `__WHALEMOUNT__`; the old vector drawing is **kept as a fallback** behind `if(!this.textures.exists('whalemount'))`, so a decode miss degrades gracefully. Alt take with no crown: `b06107fa`. | ✅ In game (megawhale ride — worlds 1/7/9/10/11/12/21). |
| Black Swan gen `9e1a6671-7940-4f59-bd90-c67fd221b621` → cutout `45969464-12be-41f5-a5cd-6ace25426b5e` → `cut_blackswan.b64` | **THE BLACK SWAN plate (10-3) at 330×360 2026-07-27** (handoff §5: it had no `cut_blackswan.b64` and fell back to a tinted ghost). Was a small 184px sprite embedded inline as `EXTRA.blackswan`; now a proper 330×360 pixel-art plate — obsidian swan, violet iridescent sheen, blade-wings spread, glowing eyes, talons. Moved from an inline data URI to `cut_blackswan.b64` via a `__BLACKSWAN__` marker (also trims ~68KB of base64 out of game_logic.js). `setScale(92/k.height)` unchanged (fills full height). Alt (smooth vector): `ffec1774`. | ✅ In game (10-3). |
| MEGA WHALE pickup icon gen `9e99ca2e-f95f-41ca-afaf-6bbe87d9ee0d` → cutout `d46f11a1-356f-48c6-974d-cc0bbb72f3b9` → `cut_megawhale.b64` | **CROWNED MEGA WHALE PICKUP ICON 2026-07-27** — the `megawhale` powerup pickup shared the plain `__WHALE__` icon; now its own crowned icon matching the ride mount (chunky cute crowned blue whale, gold trim, spout). Trimmed to its natural aspect + downscaled to 268×230 (~70KB); pickups scale by height (`setScale(40/o.height)`) so aspect is preserved. `POWERUPS.megawhale` → `__MEGAWHALE__`. The plain `whale` powerup keeps the old icon (so regular whale vs mega whale read differently). | ✅ In game (megawhale pickups). |
| Lock-celebration render `987bc451-3eb4-411a-98ab-d9c6055afa11` (alt `adfa66e1`, rejected — rooster obscured the banner + "JUPITE R LOCK" split) → lockbar composite → CDN `09ea62f3-fa88-49c0-a680-6c6e01f05796.jpg` | **CLKN LOCK CELEBRATION 2026-07-27 (+11.00M → 44.11%).** Two-vault graphic: JUPITER LOCK (436.11M, glowing) + STREAMFLOW (5.00M), Cluck hauling the +11.00M bag toward the Jupiter door, banner "44.11% OF SUPPLY LOCKED", + the ROAD TO **50%** progress bar (target raised from 40% on the owner's call — the bar hit 40% and we're past it). nano_banana_pro with the branded Cluck ref `387db388`; all plaque text came out correct first-pass (no Pillow patch needed). Posted **X first** `2081772103435481402` (image) → **silent TG** `87527` (community chat, with the X-repost link) → pending cleared. `scripts/lockbar.py` was made target-aware in the same change (it had hardcoded "40%" in the header/goal text). | ✅ Posted (X + TG). |
| ICEBERG ORDER redo `4a67f96c-7a76-4745-ae04-2649c0422786` → `w-iceberg.webp` · PROOF OF RESERVES redo `a3810326-2e6f-43de-b730-ef4b415ea2a0` → `w-proofvault.webp` (proofvault alt `8e84723b`, a domed rotunda) | **ICEBERG + PROOF-OF-RESERVES SECRET ROOMS REDRAWN 2026-07-27** (owner spotted them in-game: iceberg was muddy/unreadable, proofvault looked plain vs the other plates). Iceberg is now a clear majestic glowing iceberg (lit tip + submerged mass below the black waterline — the "iceberg order" read); proofvault is a grand marble reserve hall with an open gold vault of bullion. nano_banana_pro 1376×768, WebP q82, same house style. **Same filenames** (bgArt keys wIceberg/wProofVault unchanged → no floor-tile trap, both stay stone), so `WORLD_ART_VER` bumped **4→5** to bust the 7-day CDN cache. Verified: both rooms boot, files serve at v5. | ✅ In game (ICEBERG / PROOFVAULT bonus rooms). |
| `b321b509-cc5a-440c-bc00-ff04004cc66e` → cutout `1812c069-9f71-4680-bad2-5723b6e07051` | **ASTRONAUT NORMIE — DUCK frame (5th pose) 2026-07-25.** The moon suit shipped with FOUR frames (idle/run1/run2/jump) but the player has FIVE — `nduck` lives in the `EXTRA` map as an inline data URI, not a build marker, so it was missed and crouching on the moon showed un-suited Normie. ⚠️ **Prompting note:** a tall 2:3 canvas biases the model to draw a STANDING figure — three attempts on 2:3 (incl. one using the real duck sprite as a pose reference) all came back standing. Switching to a WIDE 4:3 canvas and asking for "wider than he is tall / compact crouched shape" produced a true crouch first try. Alt take `979dd874` lost the sunglasses; `b321b509` kept them. Normalised into the ORIGINAL nduck frame's alpha bbox so the crouch shares its ground line. Shipped as `cut_normie_moon_duck.b64`. | ✅ In game (`mnduck`). |
| _(no new art — level-data + physics change)_ | **WORLD 21 REBUILT AS MOON-NATIVE TERRAIN 2026-07-25.** The three moon levels had shipped as re-skinned pre-moon layouts — 21-1 and 21-2 carried the **identical wall array**, and all three reused the generic brick-rhythm skeleton with `moonrock` swapped in. Art and difficulty were tuned; the ground plan was not. Now: **LOW GRAVITY** (`def.grav:540` vs the default 900 — new per-level physics property, also on MOONCACHE), plus distinct landforms per level — 21-1 shallow craters + terraces, 21-2 a mare cut by narrow rille clusters, 21-3 dense crater chains into a summit climb and a deliberately flat boss arena. **Three craters are left UNBRIDGED** (260–280px) so the low gravity is load-bearing rather than cosmetic: they are impossible at earth gravity and comfortable at 540. ⚠️ Note for future level work: `DJUMP_H` (205px) is already ~the full 270px screen height, so low gravity can only ever be exercised **horizontally** — a "high ledge only reachable on the moon" is geometrically impossible. | ✅ In game (21-1/2/3 + MOONCACHE). |
| `w02-casino.png` (edited in place, no new generation) | **CASINO GIBBERISH SIGN REPAIRED 2026-07-25.** The plate carried one malformed neon sign ("CRDD" — the model's attempt at CRAPS), the last known text artifact in the set. Everything else on it is clean and correctly spelled (CASINO / SLOTS / HOTEL), so it was repainted LOCALLY rather than regenerated — a regeneration would have risked a good plate for one small defect. The letters were replaced with an abstract three-bar marquee, which is what background signage reads as at game scale anyway. Technique worth reusing: the edit is **colour-masked, not a rectangle blit**, so the building that overlaps the sign is preserved with no hand-masking. Two gotchas hit on the way — (1) masking on "is this pixel amber" left GHOST LETTERS, because a neon glyph carries a dark red halo that fails the amber test, so the whole panel must be cleared instead; (2) a colour-only occlusion test fired on dark pixels *inside* the panel and cut purple slivers through the new bars, so it must be gated geometrically too. Script kept at `scratchpad/fix_casino_sign.cjs`. **`WORLD_ART_VER` bumped 2 → 3.** | ✅ In game (2-2). |
| _(no new art — code-drawn tiles)_ | **FLOOR TILES PER WORLD 2026-07-25.** The grass-capped `ground` tile was the ONLY floor in the game, so a castle keep, a casino, a mine shaft, a bank vault and an orbital deck all grew bright green grass — the same class of bug as grass on the moon, just never noticed. Every backdrop plate was eyeballed at its ground line: **grass is correct on exactly TWO of them (`w01dusk`, `w07farm`)**. Added three code-drawn 24×24 tiles built the same way as `ground` (same speckle/highlight rects, so they read as a family) — `stoneground` (indoor/hard), `sandground` (desert), `iceground` (diamond + pod) — selected by `groundKeyFor(def)`, keyed on **bgArt** rather than theme number. Grass is now the exception and **stone is the fallback**, so a new world with a new plate gets a neutral floor instead of grass sprouting in a vault. Bridge planks and pegs follow the level's floor too (they were hardcoded to grass). `floorKey` is exposed via `__NQ_DBG` so the floor can be asserted in tests rather than eyeballed. | ✅ In game (all worlds). |
| `3f87f0f5-3857-4a84-b8a7-bb849155303d` → `w-printer.png` · `0272564c-7f35-4a34-976c-ba1e53f32bb2` → `w-diamondvault.png` | **MONEY PRINTER + DIAMOND VAULT BONUS-ROOM PLATES 2026-07-26** (owner ask: "the money printer level is blank, needs a better background, cash registers gold bars" / "diamond vault needs background of a diamond vault an diamond hands up high in the room"). Both rooms had been sharing the generic `wVaultRoom` plate with five other bonus rooms. nano_banana_pro, 16:9, native **1376×768** — the exact plate size, so no resampling. PRINTER: underground printing hall, brass cash registers along a long counter, cast-iron presses, banknote sheets drying on wire lines, glowing gold-bar pyramids in arched alcoves. DIAMONDVAULT: glacial vault chamber, two colossal clenched **diamond fists raised high** dominating the upper half, rainbow caustics, steel vault door with a cut-diamond centre, gem plinths. Prompted with an explicit "bottom third is EMPTY OPEN FLOOR" clause so the gameplay band stays clear, plus a hard no-text/no-logo clause (the casino "CRDD" lesson). 2 candidates each; the rejected diamond alternate had **disembodied floating fists** and a near-black floor band. `groundKeyFor` sends wDiamondVault to `iceground`; the printer hall keeps the stone fallback. **`WORLD_ART_VER` deliberately NOT bumped** — these are NEW filenames, not in-place replacements, so no cached plate can go stale, and a bump would needlessly re-download every other plate for every returning player. | ✅ In game (PRINTER, DIAMONDVAULT). |
| _(no new art — verification method)_ | **HOW THESE WERE CHECKED 2026-07-26.** Judged at the size the player actually sees, not at full res: the backdrop is drawn with `setDisplaySize(cam.width, cam.height)` on a 480×270 camera, so each plate was downsampled to 480×270, the in-game 0.22 black scrim composited on, then nearest-upscaled — that is what reaches the screen. It is the check that matters for text artifacts: the printer's cash-register displays carry faint marks at full res, but at 480 wide they dissolve into texture, unlike the casino's large neon sign which stayed readable. Brightness was measured rather than eyeballed, over the mid-band where sprites move: the new diamond plate reads 158 against existing shipped plates at 182 (`w06-depeg`, `w15-diamond`), so it is bright but NOT an outlier; the printer reads 50, right in the `w-vaultroom` (45) family. |

| `9cba16ee` → `w-seedvault.png` · `0c81da9e` → `w-sybilbunker.png` · `076e3a08` → `w-leveragedn.png` · `8f10d7f8` → `w-bondcurve.png` | **BONUS ROOMS RENAMED + OWN PLATES 2026-07-26** (owner: "analyze what we have and make it the best it can be, crypto related when possible"). Four rooms were still sharing the generic `wVaultRoom`. Names were derived from what each room ACTUALLY contains, not decoration: VAULT = 2 caches + 1 jackpot slot → **THE SEED VAULT** (wall of engraved brass plaques, a shrine of numbered word-slots); BUNKER = 3 airdrops → **THE SYBIL BUNKER** (concrete bunker walled with racks of IDENTICAL glowing terminals, crate on a parachute through a ceiling hatch); DEN = 3 slot machines → **THE LEVERAGE DEN** (smoky basement, three slot cabinets matching the three real slots, wall of red plunging charts); LAUNCHPAD = 4 plats + the game's biggest cache (800) → **THE BONDING CURVE** (launch gantry, a literal luminous S-curve sweeping up the frame, rocket venting). TRENCHES kept its art (already fits) and was renamed **NO MAN'S CHAIN**. All five subs are now ≤17 chars — "THE SPEAKEASY VAULT" (19) and "THE AIRDROP BUNKER" (18) had been silently truncated on the level-select card. nano_banana_pro, 2 candidates each, native 1376×768. Rejected: the scattered-screens sybil variant (rack rows read as "identical machines" far better). Brightness over the sprite band: seed 73, sybil 106, leverage 40, curve 86 — all between `w-vaultroom` (45) and `w15-diamond` (182), so none is an outlier. **`WORLD_ART_VER` NOT bumped** (new filenames, not in-place replacements). | ✅ In game (VAULT, BUNKER, DEN, LAUNCHPAD, TRENCHES). |
| `6fccabdb` → `w-orecache.png` · `6e1f060b` → `w-swanroost.png` · `74a70b0a` → `w-mempoolcache.png` · `afb1ae0d` → `w-whalegrotto.png` · `8357fb34` → `w-mooncache.png` | **THE LAST FIVE BONUS ROOMS GET THEIR OWN PLATES 2026-07-26** (owner: "all hidden worlds should have unique backgrounds" — overriding the earlier call to let these five keep their parent world's art). **All 12 hidden rooms are now on 12 unique plates.** ORE CACHE: mine chamber, a blazing gold-ore seam in the rock face, pit-props, spilled cart, hanging lanterns. SWAN ROOST: storm-lashed tower top, colossal black-feather nest cradling glowing eggs, lightning, ruined battlements. MEMPOOL CACHE: vaulted server crypt, hundreds of translucent cyan data-blocks suspended in a queued lattice. WHALE GROTTO: frozen sea-cave, a whale skeleton embedded in backlit turquoise ice, icicles, frozen treasure. MOON CACHE: buried lunar bunker, blast doors open on the regolith with Earth hanging in a black starfield. nano_banana_pro, native 1376×768, same "bottom third is EMPTY OPEN FLOOR" + no-text clauses. Brightness over the sprite band: 38 / 57 / 87 / 151 / 154, all inside the shipped 32–182 range. ⚠️ **FLOOR-TILE TRAP:** `groundKeyFor` keys off `bgArt`, so repointing a room can silently change its floor. WHALEGROTTO was on `w13pod` → `iceground` and had to be re-listed under `wWhaleGrotto` (its new plate is an ice cave, so ice is still right); MOONCACHE is safe because `def.moon` is checked BEFORE bgArt; the other three were already on the stone fallback. Verified at runtime — all 12 floors unchanged. **`WORLD_ART_VER` NOT bumped** (new filenames). | ✅ In game (all 12 bonus rooms). |
| `512c7cb0-e288-4660-8034-7676fd1ba230` → cutout `adcfb1c7-fb38-432d-8b50-0c050798789e` → `cut_dirtywhale.b64` | **THE DIRTY WHALE BOSS SPRITE 2026-07-26** (owner: "the whale boss thing is broken and needs a much different graphic"). 12-3 THE WHALE CARTEL was the one fight running on PLACEHOLDER ART: it borrowed the `whale` POWERUP icon, blew it up ~5x from a small pickup and tinted it grey (`0x6a8899`) to fake menace. Every other boss has a purpose-drawn plate. Now it has its own — a pinstriped gangster whale, cigar, gold chain, barnacles, scars, money sack. nano_banana_pro on a flat-green key, background removed, trimmed and fitted to the same **330×360** canvas as the other boss plates. **No tint applied** (`0xffffff`): the plate is already the right colour and tinting a finished sprite is exactly what made the old one muddy. Wired in all THREE places that drew the borrowed icon — the fight (`startWhaleBoss`), the win screen, and the locked-content preview card. Verified at runtime: `tex:'dirtywhale'`, untinted, still stompable. | ✅ In game (12-3). |
| Rug King `1886e59a-a759-46f7-bba7-f4c06c7a448c` → `cut_boss.b64` · Scammy KOL `cd316a35-969d-4783-a257-f95f33d2e8d8` → `cut_scammykol.b64` · Custodian `88a7e38a-21d5-41da-88e9-308dede7b1a9` → `cut_ceoboss.b64` · Vault Wyrm `6da9bafa-00e0-41b4-907c-b78ac615aedb` → `cut_wyrm.b64` · Hash Lord `256ffe3b-3065-4634-9146-7918c95ca828` → `cut_golem.b64` · MEV Dragon `8a7f3b86-f5d8-4b86-9299-353871eccdde` → `cut_mevdragon.b64` · Troll `d8e1dc20-0438-48bd-8aae-3a6578713577` → `cut_troll.b64` · Sentry turret `f4593e27-a8d3-4566-a8ef-b59b34ea7f4b` → `cut_sentry.b64` | **EIGHT NORMIE QUEST BOSS/ENEMY PLATES AT THE 330×360 STANDARD 2026-07-27** (commits `3568707` Rug King/Troll/MEV Dragon, `c56b078` Scammy KOL/Custodian/Vault Wyrm/Hash Lord, `e6be802` sentry). All nano_banana_pro on a flat-green key, background removed, trimmed and fitted to the same **330×360** canvas as the Dirty Whale, **untinted** (`0xffffff`). Dimensions: all **330×360** except the sentry (**330×385**). Design notes: Rug King = gaudy toad-monarch yanking a rug; Scammy KOL = shill in mirrored aviators + ring-light "fake saint" halo; Custodian (4-3, `ceoboss` texture) = faceless corporate monster whose HEAD IS A VAULT DOOR — deliberately NOT a caricature of a real exchange CEO; Vault Wyrm (5-3, `wyrm`) = chain-link serpent; Hash Lord (`golem`) = golem of stacked mining rigs, **reused verbatim as 9-3's boss so one plate upgrades two fights**; Troll = keyboard-goblin; MEV Dragon (`mevdragon`) = data-block dragon — **previously it borrowed the Vault Wyrm's `wyrm` texture tinted purple (two fights six worlds apart were the same creature); now its own plate**. Sentry turret (`sentry`) = buried gun-pod, which **replaced the burrowing worm everywhere EXCEPT world 2's 2-1/2-3** (owner's child's world — the miniworms stay worms there, by the `_mwKey` split). CDN host for all: `d8j0ntlcm91z4.cloudfront.net/user_3FusN3Ju3eYDQtXzIkwBTR7xnLV/` (find each via `show_generations` by job ID). | ✅ In game. |
| Yield Reaper gen `7ebdc5c7-3992-4f00-b038-7fbc9b98a6f8` → cutout `3c664672-4ebb-48a1-812a-f7305343c6e4` → `cut_reaper.b64` · Great Bear gen `6910471a-6d37-4d1d-be30-9e543460b2f4` → cutout `ac26be5d-7215-4c0c-ba4a-0eeb4f96d13e` → `cut_greatbear.b64` | **YIELD REAPER (7-3) + GREAT BEAR (8-3) UPGRADED TO 330×360 2026-07-27** — the last two boss plates that were below the standard (Reaper was 223×220, Bear 227×224; handoff §5). nano_banana_pro on a flat-green key, background removed, contain-fit to 330×360. **Both fill the full 360 height**, so the in-game `setScale` TARGETs (reaper 88, bear 104) display them at the SAME on-screen size as the old full-bleed plates — **no scale-code change needed**. Untinted (`0xffffff`). Reaper = skeletal DeFi yield-farmer: straw hat, scythe blade of green candlesticks, staking-receipt bandolier + robe, coin-leaking hourglass, spilling coin sack. Great Bear = bear-market grizzly: red-candlestick fur, red down-arrow claws, red crash-candle breath, torn green BULL-merch scrap, descending chart-scar, crushing a golden bull. CDN host `d8j0ntlcm91z4.cloudfront.net/user_3FusN3Ju3eYDQtXzIkwBTR7xnLV/`. | ✅ In game (7-3 / 8-3). |
| VIP-world boss plates: `cut_leviathan.b64` 320×360 · `cut_burnlord.b64` 271×360 · `cut_diamondtitan.b64` 314×360 · `cut_coresentinel.b64` 288×360 · `cut_marketmaker.b64` 295×360 · `cut_chairman.b64` 277×360 · `cut_satwarden.b64` 336×360 · `cut_stormherald.b64` 327×360 (prior-session generations; job IDs not captured — recoverable via `show_generations`) | **VIP-WORLD BOSS ROSTER — dimensions recorded 2026-07-27** (handoff §5: "19 of 22 bosses have no MEDIA_LIBRARY row"). The `VIP_BOSSES` table (game_logic.js) drives worlds 14–20 via the shared `startVipBoss` / `vipBossBomb` mechanic; each is a purpose-drawn ~×360-height plate, untinted, stompable ×4–5: THE LEVIATHAN (abyssal whale-king), THE BURN LORD, THE DIAMOND TITAN, THE CORE SENTINEL, THE MARKET MAKER, THE CHAIRMAN (money-printer Fed), THE SATELLITE WARDEN, THE STORM HERALD. WEN MOON (W21 finale, 330×360) has its own row above. | ✅ In game (worlds 14–20). |
| `cut_saylor.b64` 256×360 (prior session; job ID not captured) | **THE SAYLOR — bitcoin-maxi boss, dimensions recorded 2026-07-27.** World-20 "Saylor Summit" boss; attack `saylorBomb` (orange leverage bombs). Purpose-drawn 256×360 plate, untinted. | ✅ In game. |
| `cut_wormhole.b64` 189×230 | **THE WORMHOLE (2-3 boss) — DO NOT TOUCH. Owner's child's art.** Recorded for completeness ONLY. It is deliberately smaller/simpler than the 330×360 plates and that is CORRECT — per CLAUDE.md + HANDOFF §2 it must never be replaced, redrawn or resized; an art audit that calls it below-standard is wrong. `bossType:'wormhole'`, texture `wormhole`, `cut_wormhole.b64`. | ✅ In game (2-3) — locked by owner directive. |
| `2529bb9a` → `w-deadwallet.png` · `841586f3` → `w-coldstorage.png` · `d6647e7e` → `w-darkpool.png` · `a59b0986` → `w-iceberg.png` · `75c265c1` → `w-proofvault.png` · `663c02ec` → `w-satstation.png` · `207fe29b` → `w-supercycle.png` | **SEVEN NEW VIP SECRET-ROOM PLATES 2026-07-26** (owner: "VIP secret rooms sound great too"). Worlds 13 and 21 each had a secret room; the seven VIP worlds between them had none, so most of the tier you PAY for had nothing to find. One room + one plate per world, so the "all hidden worlds have unique backgrounds" rule holds at **19 rooms on 19 plates**. DEAD WALLET (W14): obsidian furnace hall, a sealed strongbox door cracked with molten orange, braziers, coins fused into slag. COLD STORAGE (W15): glacier vault, ice-encased deposit lockers glowing from within, stalactites. DARK POOL (W16): subterranean chamber, a still black obsidian mirror-pool under violet vault ribs. ICEBERG ORDER (W17): flooded cavern under the bourse, a colossal iceberg with only its tip lit, neon cyan/violet on black water. PROOF OF RESERVES (W18): marble bank chamber, circular gold vault door standing open on stacked bullion. SAT STATION (W19): orbital cargo module, panoramic window with Earth in a black starfield, gold-foil pods. SUPERCYCLE (W20): summit colonnade above the storm, a golden ascending staircase, lightning below. nano_banana_pro, native 1376×768, same "bottom third is EMPTY OPEN FLOOR" + no-text/no-character clauses. **One plate was CORRECTED, not shipped as rendered:** cold storage measured **224** over the sprite band — well outside the 32–182 shipped range, bright enough to wash Normie out — so a smooth floor-shadow gradient (transparent above 36% height → 66% black at the bottom) brought it to **115** without dimming the ice cave itself. Final band brightnesses: 12 / 115 / 16 / 23 / 134 / 59 / 32. ⚠️ **FLOOR-TILE TRAP, second sighting:** `wColdStorage` was added to `groundKeyFor`'s `iceground` list (it is a glacier); `wIceberg` was deliberately NOT, despite the name — its plate's floor is a wooden pier deck, so it keeps the stone default. The other five were already correct on stone. **`WORLD_ART_VER` NOT bumped** (new filenames). | ✅ In game (DEADWALLET, COLDSTORAGE, DARKPOOL, ICEBERG, PROOFVAULT, SATSTATION, SUPERCYCLE). |
| _(no new art — code-drawn icons)_ | **AIR STRIKE + STOPWATCH INVENTORY ICONS 2026-07-26.** Two items were wearing borrowed sprites and BOTH were reported as broken, because in the world those sprites mean something else: the enemy-wipe wore the AIRDROP crate (a crate is a heart) and the +30s wore the CAFFEINE cup (caffeine is super speed). Players spent them, watched for the wrong effect, saw nothing, and called them dead. Two new code-drawn textures give them honest faces — `missile` (nose cone pointing DOWN, exhaust trailing up: a strike coming IN, not a launch) and `stopwatch`. No assets to load. | ✅ In game. |
| _(no new art — banner copy)_ | **"WORLD VAULT" FIXED 2026-07-26.** The level-intro banner printed `'WORLD '+def.name`, which is right for `4-2` but rendered as "WORLD VAULT" / "WORLD LAUNCHPAD" in the bonus rooms — reading like a missing string rather than a secret you just found. Hidden rooms now announce **★ SECRET ★**; numbered levels are unchanged. |

> ⚠️ **World plates are served with `max-age=7d` and are REPLACED IN PLACE (same filename).** A
> returning player therefore keeps the OLD art for up to a week — the worlds 1-4 retro repaint was
> live on the server (md5 verified) while browsers still showed the painted plates. **Whenever you
> replace anything in `normie-quest/public/worlds/`, bump `WORLD_ART_VER` in game_logic.js** (it is
> appended as `?v=` on the lazy load, which busts the cache without renaming files).

| `c6bedda1` w01-dusk · `df498f1a` w01-cavern · `1034aa81` w01-keep · `317381a5` w02-desert · `cafd2f83` w02-casino · `65929d8b` w03-skyline · `4d20d265` w04-exchange | **RETRO RAMP — worlds 1-4 re-done as PIXEL ART 2026-07-25** (owner: the first levels should feel "old school Mario", then "slowly ramp up on the newness"). REPLACES the painted 2026-07-24 plates for these seven themes at the same filenames/bgArt keys, so it was an image-only swap with zero code change. The ramp is deliberate and staged: **W1 = 8-bit NES** (flat colour bands, chunky clouds, rounded hills, two shades per element) → **W2 = 16-bit SNES** (dithered shading, layered parallax) → **W3-4 = detailed 32-bit/indie pixel art** (bloom, atmosphere, many parallax layers) → **W5-15 painted** → **W16-21 cinematic**. This restores the retro→modern arc that world 16's "modern image-background engine" commit established and that painting worlds 1-15 in one register had flattened. ⚠️ Resample pixel-art plates with **nearest-neighbour** (`kernel:'nearest'`) — the default lanczos downscale blurs the hard pixel edges and kills the look. | ✅ In game (worlds 1-4). |
| `9276dfbb` w09-mines · `1e926143` w10-euphoria · `936c1d45` w10-swan · `119bcc5f` w11-citadel · `c66b5134` w12-relaunch · `0947f222` w13-pod · `610788eb` w14-shrine · `b51ca20a` w15-diamond · `1c06aac6` w-trenches · `0da5aa12` w-vaultroom | **DEEP-WORLD BACKDROPS 2026-07-24** (Normie Quest worlds 9-15 + the two hidden bonus-room themes) — the second half of the art refresh, same nano_banana_pro 16:9 2k house style. Mine shaft / euphoria spires / black-swan storm / mempool citadel / dawn relaunch / abyssal whale pod / burn shrine / diamond dimension / crypto trenches / speakeasy vault room. With these, ALL 70 levels have a painted plate — no procedural-gradient worlds left. Dead job `2a954215` (diamond) stalled in_progress and was regenerated as `b51ca20a`. | ✅ In game (28 levels). |
| `43253031` w01-dusk · `0a02c7b4` w01-cavern · `482e0de6` w01-keep · `877e3927` w02-desert · `817efa9b` w02-casino · `f0eeeff1` w03-skyline · `22e80a4d` w04-exchange · `7a5872b8` w05-bridge · `4a09b571` w06-depeg · `d1c25d0c` w07-farm · `065b552c` w08-bear | **EARLY-WORLD BACKDROPS 2026-07-24** (Normie Quest worlds 1-8, owner: the early worlds "look a little out of touch with the awesome stuff lately"). nano_banana_pro 16:9 2k hand-painted mattes in the same house style as the w16-w21 plates, replacing the procedural sky/hill gradients (and the older sprite `bgImage` art on worlds 3-4, which `bgArt` short-circuits). Keyed by THEME not by world — world 1 alone spans three looks (dusk valley / teal cavern / crimson keep), so one plate per world would have flattened it. Each resized to the established 1376x768 plate size. Note: `w02-casino` carries some gibberish neon signage — illegible at in-game scale, no brand logos. | ✅ In game (24 levels across worlds 1-8). |
| Per-level backdrops, worlds 3–8 (all WebP q82, 1376×768): 3-2 `2b802d93`→`w03skyline2` · 3-3 `ea3d2828`→`w03skyline3` · 4-2 `113c1089`→`w04exchange2` · 4-3 `94c7b4fd`→`w04exchange3` · 5-2 `98581ae9`→`w05bridge2` · 5-3 `c3787eae`→`w05bridge3` · 6-2 `d65f4ad2`→`w06depeg2` · 6-3 `79dee9ea`→`w06depeg3` · 7-2 `500b1f7c`→`w07farm2` · 7-3 `6390cc54`→`w07farm3` · 8-2 `57ebc436`→`w08bear2` · 8-3 `0291fda6`→`w08bear3` | **PER-LEVEL BACKGROUNDS, WORLDS 3–8 2026-07-27** (handoff §4.2, "12 for worlds 3–8"). Worlds 3–8 previously shared ONE plate across all three levels; now level -1 keeps the world plate and levels -2/-3 each get a distinct scene, so no early world repeats a backdrop. nano_banana_pro 16:9 native 1376×768, encoded WebP q82 (avg 83KB), same "bottom third EMPTY OPEN FLOOR" + no-text/no-character house style as the 2026-07-24 backdrops. Scenes track each level's beat (THE TIMELINE / SCAMMY KOL stage / TRADING FLOOR / CUSTODIAN vault-lobby / THE SPAN / VAULT WYRM lair / DEPEG EVENT / HASH LORD forge / COMPOUND COUNTRY / YIELD REAPER harvest / CAPITULATION / GREAT BEAR den). ⚠️ **FLOOR-TILE TRAP handled:** 7-2/7-3 carry an explicit `ground:'ground'` (grass) so repointing bgArt off `w07farm` didn't silently drop them to stone; worlds 3/4/5/6/8 are stone by default (no override needed). `WORLD_ART_VER` unchanged (new keys — nothing cached to bust). CDN host `d8j0ntlcm91z4.cloudfront.net/user_3FusN3Ju3eYDQtXzIkwBTR7xnLV/`. | ✅ In game (worlds 3–8, levels -2/-3). |
| `a049c770-2eb9-4f13-a221-f2a0d353207c` | **W21 MOON backdrop 2026-07-24** (Normie Quest world art): hand-painted matte — pale cratered lunar plain, Earthrise over the horizon, flag monument, gold nebula glow. Resized 1376×768 to match the other world plates; shipped as `normie-quest/public/worlds/w21-moon.png`. Alt take `4d40e1b8` unused. | ✅ In game (`bgArt:'w21moon'`). |

| `6e34e576-8804-479a-a3e0-fc4e109299a1` + Pillow post | Lock celebration 2026-07-10 (+5.00M via Jupiter → 38.94%): two-vault scene, S→5 plaque glyph patched in post, "ROAD TO 40%" progress tracker composited (scratchpad `lockbar.py` — REUSE for every future lock post; owner loves the tracker). Final: CDN `f1a04fe3….png`. Posted X `2075577233230463231` + TG 85876. | ✅ Owner asked for "special" — tracker is now the lock-post signature. |
| `eab447dc-2fa5-4dea-baa0-bc9cc4fb210f` | **Buy Special REBUILD announcement poster 2026-07-24**: nano_banana_pro 16:9 dark-mode SaaS key art — floating glass payout dashboard (gold/dark mode-toggle pills, amber payout bars, emerald verify column), all three text strings rendered EXACTLY ("BUY SPECIAL" / "REWARD EVERY BUYER — OR RANK THE TOP" / "SCAN. VERIFY. PAY. ONE PAGE."). No mascot and no logos by design, so no reference image was needed. First roll, kept — zero typos. Posted X `2080754638094037263` + TG community 87385 (silent, with X link). CDN: `hf_20260724_203530_eab447dc-…png`. | ✅ Posted both channels. **Product-poster recipe: skip the mascot entirely and the mascot-reference rule stops applying — abstract blurred UI rows avoid gibberish micro-text while still reading as wallet addresses.** |
| `069d1a48-b675-490c-adfb-78bb8f7be880` | **ROSE 10% Buy Special party poster 2026-07-19**: nano_banana_pro 16:9 neon-noir rose-explosion poster, all three text strings rendered exactly ("ROSE 10% BUY SPECIAL" / "EVERY BUYER GETS +10%" / "ENDS TUESDAY 6PM ET"), dark+orange Cluck palette + ROSE crimson. First roll, kept. Posted: X 2078925958841565489 (+ TG mirror) and Rose community chat (-1002625127458). CDN: `hf_20260719_192838_069d1a48-…png`. Sonnet-crafted prompt per owner rule. | ✅ Posted both channels. |
| (no job — local extraction from ref `47a38e56`) | **Normie DUCK pose sprite 2026-07-19**: the DUCK frame lifted straight from the owner's NORMIE sprite sheet (ref `47a38e56`), black-on-black bg removed via edge flood-fill + silhouette closing (hoodie interior refilled #100e11), composed on the shared 144×216 frame canvas at 52% standing height, feet on the standing baseline. Embedded as `EXTRA.nduck` in normie-quest-platformer.html — renders unsquashed, replaces the squashed-idle fallback. | ✅ In-game verified (crisp at game scale, no flicker). |
| `b130eee2-bdd8-47cf-8e34-30e9b9fc28de` → cutout `f49d66a4` | **THE BLACK SWAN boss sprite 2026-07-19** (Normie Quest World 10 final boss): NES pixel-art, standing talons + blade-wings spread, violet sheen/eyes, style-matched to the owner's NORMIE sprite sheet (ref `47a38e56`). Background-removed, trimmed, 184px, embedded as `EXTRA.blackswan` in normie-quest-platformer.html. Alternate take (swimming pose, prettier but legless): `f7abd796-2571-473e-ab29-157717a30204`. | ✅ In-game verified. Swap = re-embed the alternate's cutout. |
| `2d06b14a-dc34-4ef7-aaab-89bbf6788211` | **Cluck Norris BOT avatar 2026-07-17** (robot remix of the main logo, ref `b1f7713b`): plasma-blade mohawk, black wraparound visor w/ orange scanline, graphite-steel armor with glowing orange seams, capsule bandolier, hex-plate feathers, fire ring + flaming "CLUCK NORRIS" wordmark (rendered PERFECTLY by nano_banana — rare). CDN `hf_20260717_141332_2d06b14a….png`. Sent to owner's private bot chat (msg 608). | ✅ Owner: "perfect." THE bot-brand avatar. |
| `cc4707c4-4cc1-409d-b36e-68ad52f836b2` | Same robot Cluck, TEXTLESS emblem variant (no wordmark, avatar-crop-safe, faint circuit ring behind flames). CDN `hf_20260717_141415_cc4707c4….png`. | ✅ Kept as the no-text companion (profile pics / overlays that get their own type). |

| Asset | Where | What |
|---|---|---|
| `docs/brand/logo-strip-top6.png` | repo (committed) | 6 official top-mcap coin logos in gold-ringed discs: BTC · ETH · BNB · XRP · SOL · TRX. For video overlays/end-cards. Real logos — AI must NEVER draw these. |
| `normie-quest/public/worlds/w16-citadel.png` (job `17b49783`) | repo (committed 2026-07-22) | **World 16 modern background** — full-bleed hand-painted cyber-citadel (neon skyline + riveted vault wall + orange conduits + stone ledge), the anchor for the retro→modern art transition (worlds 16-21, per owner: "slightly more modern as we go up from 15"). First roll `c9c85f3e` was rejected (nano rendered it inside a TV bezel + gibberish ticker text); re-rolled full-bleed/text-free. Not yet wired into the game as a bgImage. |
| `public/learn-icons/*.png` | repo (live) | 18 official coin logos (CoinGecko art) used on /learn pages. |
| `docs/brand/dexscreener-banner-600x200.{png,webp}` | repo (committed 2026-07-21) | The LIVE DexScreener header banner (fiery branded Cluck + dynamite, ETH/BTC/SOL discs, wordmark + clucknorris.app pill). Recovered from DexScreener's CDN — it had never been archived. Source of truth for listing-site banners (Bags etc.). |

## Hard-won generation rules (save credits, save patience)

- **AI cannot render real coin/brand logos.** It duplicates and hallucinates. Real logos go in
  via ffmpeg overlay on a CLEAN plate, or a static end-card — never in the prompt.
- **Branded mascot = always pass a reference image.** Text-only descriptions produce generic
  "play chicken" output the owner rejects.
- **NSFW false-flags refund automatically** — a flagged render costs nothing but time. Don't
  water down the mascot to avoid flags; just drop weapon-adjacent words from the text.
- Costs (ULTRA plan as of 2026-07-17 — old "Plus 1200/mo" note stale): 15s 1080p high-bitrate
  Seedance ≈ 135 credits; nano_banana image ≈ 2. Most X posts should carry an image (cheap).
- **Free real-Cluck voice dubs:** prod `POST /api/tts {text,lang}` returns branded-voice mp3
  (cached forever server-side) — dub over generated video with ffmpeg (duck native audio to
  ~0.30, adelay the line). Zero Higgsfield credits, zero new ElevenLabs cost after first synth.
- ffmpeg lives at `python3 -c "import imageio_ffmpeg; print(imageio_ffmpeg.get_ffmpeg_exe())"`
  (pip `imageio-ffmpeg`; apt ffmpeg 404s in this container). Static-PNG overlay needs
  `-loop 1 -t <dur>` or fade filters keep alpha at 0.

| 2026-07-21 | NORMIE PRINCESS candidate A2 (dark dreads, NORMIE patch) | nano_banana_pro | e008bcad-c46e-41c6-88b4-fcdb10a7d2bc | https://d8j0ntlcm91z4.cloudfront.net/user_3FusN3Ju3eYDQtXzIkwBTR7xnLV/hf_20260721_132857_e008bcad-c46e-41c6-88b4-fcdb10a7d2bc.png | REJECTED (owner picked B2) |
| 2026-07-21 | NORMIE PRINCESS candidate B2 (honey dreads, tilted crown, NORMIE patch) | nano_banana_pro | dd41d712-2a18-4319-9698-4c44db0e4f93 | https://d8j0ntlcm91z4.cloudfront.net/user_3FusN3Ju3eYDQtXzIkwBTR7xnLV/hf_20260721_132906_dd41d712-2a18-4319-9698-4c44db0e4f93.png | ✅ KEPT — owner pick; bg-removed (job 9af56c75-0d04-485a-9a7c-60c0440f312a), 160x216 sprite embedded as EXTRA.princess in the game |
